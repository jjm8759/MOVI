// import express from 'express';
// import { deleteUsers, getUsersById, getAllUsers, getUserEmail, putUsers } from '../actions/usersLogic.js';

// const router = express.Router(); // Get access to express router

// // root at localhost:5000/users
// router.get('/:id', (req, res) => getUsersById(req, res));
// router.get('/', (req, res) => getAllUsers(req, res));
// router.get('/email', (req, res) => getUserEmail(req, res));
// router.put('/:id', (req, res) => putUsers(req, res));
// router.delete('/:id', (req, res) => deleteUsers(req, res));

// export default router;